name: sample

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  sample:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v3
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ap-northeast-1
      
      - name: Create GitHub Apps Token
        uses: actions/create-github-app-token@v1
        id: app-token
        with:
          private-key: ${{ secrets.PRIVATE_KEY }}
          app-id: ${{ vars.APP_ID }}
      
      - name: Send command to EC2 via SSM
        run: |
          aws ssm send-command \
            --document-name "AWS-RunShellScript" \
            --instance-ids ${{ secrets.INSTANCE_ID }} \
            --comment "Clone repo using GitHub App token" \
            --parameters 'commands=[
              "export TOKEN=${{ steps.app-token.outputs.token }}",
              "git config --global url.\"https://x-access-token:$TOKEN@github.com/\".insteadOf \"https://github.com/\"",
              "git clone https://github.com/ty202212/myapp2.git /tmp/my-repo"
            ]' \
            --output text \
            --region ap-northeast-1
